# Core app
streamlit>=1.50
pandas>=2.0
numpy>=1.24
joblib>=1.3

# Model
xgboost>=2.0
scikit-learn>=1.3

# Charts
plotly>=5.20

# Optional: enables true per-patient ("local") explanations in the
# Model Insights tab. If not installed, the app automatically falls
# back to global feature importance instead of crashing.
shap>=0.44
